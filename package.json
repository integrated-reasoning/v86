{
  "name": "v86",
  "version": "0",
  "license": "BSD-2-Clause",
  "scripts": {
    "test": "mocha tests/browser/*.js",
    "test:watch": "mocha tests/browser/*.js --watch"
  },
  "devDependencies": {
    "jsdom": "^22.1.0",
    "mocha": "^10.2.0",
    "sinon": "^17.0.1"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "transform": {
      "^.+\\.js$": "babel-jest"
    },
    "moduleNameMapper": {
      "^@/(.*)$": "<rootDir>/src/$1"
    }
  }
}
